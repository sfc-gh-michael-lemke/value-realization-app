[build-system]
requires = ["flit_core"]
build-backend = "flit_core.api"

[project]
name = "value_realization_app"
version = "0.1.0"
requires-python = "~=3.11"
dependencies = [
    "python-pptx",
    "requests",
]
